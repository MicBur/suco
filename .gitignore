# ---- vendored sources built locally (Windows/MinGW zstd) ----
thirdparty/

# ---- build artifacts ----
build/
build_*/
*.o
*.obj
*.i
*.ii
*.a
*.so
*.deb
*.rpm
*.tar.gz
*.tar.zst
*.zip
gcm.cache/
*.gch
*.pch

# ---- editor / OS noise ----
.DS_Store
*.swp
*~
nohup.out
*.log

# ---- internal working docs (NOT part of the product) ----
CLAUDE.md
ROADMAP-IB.md
FIX_REPORT.md
brain-ag.md
benchmark_results.md
benchmarks/
massive_benchmark/
scratch/
.agents/

# ---- deployment specific to a private grid (contains host topology + password) ----
# These target one specific private LAN (baked-in credentials + static host IPs).
# Users deploy their own way (apt install + systemctl enable). Publish sanitized
# templates later if wanted.
scripts/deploy_grid.sh
scripts/enable_auth.sh

# ---- misc scratch ----
# (removed overbroad d:* )
# (removed overbroad d*/ — matched docs/)
test.cpp
test.o
test_compile.cpp
benchmark_large.cpp
benchmark_large.obj

# personal benchmark tooling (targets a specific grid)
run_massive_benchmark.sh
run_massive_benchmark.ps1
generate_benchmark.ps1
d??comp?scratch?qt_test/

brain-claude.legacy.local

# --- build / packaging / test artefacts (generated; ~400 MB, never commit) ---
# packaged release tree, installer test output, RPP scratch project
dist/
test_install_dir/
test_rpp/
# downloaded build tooling (NSIS, 7zr) and local agent build notes
tools/
memory/
# generated 101-TU benchmark corpus and its build dirs
large_bench_project/
suco_test_log.txt
# built installers
packaging/windows/*.exe
# CMake generates this into build/generated, not into the source tree
src/common/suco_version.h
